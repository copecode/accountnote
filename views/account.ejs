<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>记账</title>
  <style>
    * {
      box-sizing: border-box;
      line-height: 1.5em;
    }
    body {
      width: 60%;
      margin:0 auto;
    }
    h1,.actform{
      width:100%;
      margin:0 auto
    }
    .actform * {
      display: block;
    }
    .ipt{
      width: 100%;
      height: 2.5rem;
      outline: 1px solid #f6f2f28e;
    }

    .ipt.ps {
      height: 6rem;
    }
    .lbl{
      font-weight: bolder;
    }


        .ipt:focus {
            /* 当获得焦点时，去除默认的灰色边框 */
            outline: none;
            border: none;
            /* 通过box-shadow属性来模拟蓝带毛绒效果，这里通过多层阴影叠加营造毛绒感 */
            box-shadow: 0 0 3px 1px rgba(113, 113, 205, 0.7),
                        0 0 5px 2px rgba(113, 113, 205, 0.5),
                        0 0 7px 3px rgba(113, 113, 205, 0.3);
                        /* 添加过渡效果，让边框样式改变时有过渡动画，时长0.1秒 */
                        transition: border 0.1s, box-shadow 0.1s;
        }
        input[type="date"] {
          display: flex;
          align-content: flex-start;
        }
        .btn {
         background-color: rgba(113, 113, 255, 0.7)
        }
  </style>
</head>
<body>
  <h1>添加记录</h1>
  <form class="actform" action="/account" method="post" name="accounting">
    <label for="acc-ipt" class="lbl">事项</label>
    <input type="text" class="ipt reason" id="acc-reason"  name="reason">
    <label for="acc-time" class="lbl">发生时间</label>
    <input type="date" class="ipt time" id="acc-time" name = 'time' >
    <label for="acc-type" class="lbl">类型</label>
    <select  class="ipt type" id="acc-type"  name="type">
      <option value="1">收入</option>
      <option value="2">支出</option>
    </select>
    <label for="acc-value" class="lbl">金额</label>
    <input type="text" class="ipt value" id="acc-value" name = 'value'>
    <label for="acc-ps" class="lbl">备注</label>
    <textarea type="text" class="ipt ps" id="acc-ps" name = 'ps'></textarea>
    <br>
    <button class="btn ipt">添加</button>
  </form>
</body>
</html>